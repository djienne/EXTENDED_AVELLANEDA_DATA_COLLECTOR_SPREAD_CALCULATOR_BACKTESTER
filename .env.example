# Extended DEX Data Collector - Environment Variables
# Copy this file to .env and fill in your values
#
# Usage:
#   cp .env.example .env
#   nano .env  # Edit with your credentials
#
# Note: .env is gitignored - never commit credentials to git

# =============================================================================
# API Authentication (Optional - only needed for authenticated endpoints)
# =============================================================================

# API Key from app.extended.exchange
# Required for: authenticated WebSocket streams, private account data
# Optional for: public orderbook and trades (works without authentication)
API_KEY=your_api_key_here

# Alternative key name (some configurations use this)
EXTENDED_API_KEY=your_api_key_here

# =============================================================================
# Environment Configuration
# =============================================================================

# Exchange environment: mainnet or testnet
# Default: mainnet
EXTENDED_ENV=mainnet

# =============================================================================
# Logging Configuration
# =============================================================================

# Rust logging level
# Options: trace, debug, info, warn, error
# Default: info
#
# Examples:
#   RUST_LOG=info         # Standard logging
#   RUST_LOG=debug        # Verbose debugging
#   RUST_LOG=extended_data_collector=debug,tokio_tungstenite=info  # Module-specific
RUST_LOG=info

# =============================================================================
# Starknet Keys (Optional - for L2 operations)
# =============================================================================

# Starknet public key (hex format without 0x prefix)
STARK_PUBLIC=

# Starknet private key (hex format without 0x prefix)
STARK_PRIVATE=

# L2 Vault number
VAULT_NUMBER=

# =============================================================================
# Pacifica Integration (Optional - for SOL markets)
# =============================================================================

# Solana wallet address
SOL_WALLET=

# Public API key
API_PUBLIC=

# Private API key (keep secure)
API_PRIVATE=
